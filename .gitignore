# Local .terraform directory, which contains the Terraform modules and providers.
.terraform/

# Terraform state files.
*.tfstate
*.tfstate.*

# Crash log files.
crash.log

# Local override files (e.g., tfvars).
override.tf
override.tf.json

# Configuration files containing sensitive data.
*.tfvars

# .terraform.lock.hcl is used to lock provider versions, which shouldn't be tracked by Git.
.terraform.lock.hcl

# Backup files generated by Terraform.
*.bak
