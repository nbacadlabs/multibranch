pipeline {
    agent {
        docker{ image 'vakem/kubepodtemplate:latest'}
    }

    stages {
        stage('Test') {
            steps {
                sh 'az --version'
                sh 'terraform --version'
            }
        }
    }
}