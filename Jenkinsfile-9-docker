pipeline {
    agent {
        docker{ image 'vakem/kubepodtemplate'}
    }

    stages {
        stage('Test') {
            steps {
                sh 'az --version'
                sh 'terraform --version'
            }
        }
    }
}