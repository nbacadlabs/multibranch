pipeline {
    agent {
        docker{ image 'vakem/kubepodtemplate'}
        label 'my-label'
    }

    stages {
        stage('Test') {
            steps {
                sh 'az --version'
                sh 'terraform --version'
            }
        }
    }
}