---
- hosts: localhost
  connection: local
  collections:
    - azure.azcollection
  tasks:
    - name: Creating resource group
      azure_rm_resourcegroup:
        name: "nbtest-rg"
        state: absent
        force_delete_nonempty: yes
        location: "eastus"